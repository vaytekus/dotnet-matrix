# dotnet-matrix

Console app that performs matrix operations and prints the result with highlighted elements.

## Stack

- .NET 8 / C#
- BenchmarkDotNet

## Run

```bash
dotnet run --project src/Matrix.Application
```

## Benchmark

```bash
dotnet run --project src/BenchmarkDotNet.Application -c Release
```